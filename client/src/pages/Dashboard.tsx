import React from 'react';
import { Box, Typography } from '@mui/material';

const Dashboard: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Панель пользователя</Typography>
      <Typography>Личная панель - в разработке</Typography>
    </Box>
  );
};

export default Dashboard; 